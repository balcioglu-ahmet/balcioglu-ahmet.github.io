# Optional per-page author profiles.
#
# The site uses `site.author` from `_config.yml` by default. Add entries here
# only if a page needs `author: some-key` front matter for a different profile.
{}
